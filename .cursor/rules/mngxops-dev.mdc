---
description: mngxops 开发规范（Python / 注释 / 变更原则）
alwaysApply: true
---

# mngxops 开发规范

- **语言与注释**：Python 3.9.6；PEP 风格；注释与函数 docstring 使用中文，函数须有一句话说明。
- **结构**：可读可维护；函数单一职责；避免重复、过长函数与过深嵌套。
- **环境**：本机默认 venv 为 `D:\PyCharm\venv3`（CI / 其他机器不受此路径约束）。
- **变更原则**：在既有功能上扩展时，未经确认不改动业务逻辑；修改公共方法须同步全部调用方，保证全局一致。