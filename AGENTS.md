# mngxops 优化点台账（Q1–Q159）

本文档记录历史优化点结论，供 Agent / 开发快速检索。**不修改业务逻辑时请以本文件结论为准。**

完整软件需求设计见 [`docs/README.md`](docs/README.md)。本文件为优化点结论的**唯一**台账。

## 状态图例

| 状态 | 含义 |
|------|------|
| 已完成 | 需求已落地 |
| 已关闭（结论） | 经确认不做或维持现状（有明确结论） |
| 已关闭（不做） | 并入父任务，本条目不单独实施 |

## 总览

### 配置管理 / 配置同步

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q1 | 返回列表保持节点展开 | 已完成 |
| Q2 | 创建配置标签后跳转绑定页 | 已完成 |
| Q3 | 绑定节点改为弹窗多选 | 已完成 |
| Q4 | 绑定弹窗交互与未绑定标签区 | 已完成 |
| Q5 | 未绑定标签不做批量/查询 | 已关闭（结论） |
| Q6 | 删除模板备注占位文案 | 已完成 |
| Q7 | 未同步绑定解除改为物理删除 | 已完成 |
| Q8 | 同步失败标黄跳转与任务关联 | 已完成 |
| Q9 | 单节点同步真实进度轮询 | 已完成 |
| Q10 | 同步进度曾用读秒（由 Q39 取代） | 已完成 |
| Q11 | 同步页手动添加限定当前节点 | 已完成 |
| Q32 | 配置列表节点展示对齐发布中心 | 已完成 |
| Q44 | 版本对比返回链接 404 | 已完成 |
| Q99 | 配置列表「全部」状态标签默认可见 | 已完成 |
| Q103 | 解除绑定文案/弹窗宽度与 marked_deleted 同步异常 | 已完成 |
| Q104 | 同步进度遮罩阶段与实时结果树 | 已完成 |
| Q105 | 同步结果树含跳过明细；任务详情默认展开 | 已完成 |
| Q114 | 离线/未知节点禁止同步与发布/回滚 | 已完成 |
| Q127 | 周期性自动配置同步不做 | 已关闭（结论） |
| Q150 | SSH/Nginx 双维度状态与门禁（卸载后 orphaned） | 已完成 |

### 发布中心 / 发布历史

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q12 | 发布中心/任务中心/历史按节点配置关联重构 | 已完成 |
| Q13–Q21 | 发布中心交互与视觉迭代 | 已完成 |
| Q23–Q27 | 确认清单、查询、移除传统创建页 | 已完成 |
| Q31 | 刷新后绑定加载卡住 | 已完成 |
| Q45–Q48 | 完整日志批次过滤、搜索与首发备份 | 已完成 |
| Q49–Q59 | 回滚能力与历史列表重构 | 已完成 |
| Q61 | 备份按 hostname 分子目录 | 已完成 |
| Q79 | 发布详情布局对齐任务详情 | 已完成 |
| Q80 | 发布 SSH 复用、同节点统一 reload | 已完成 |
| Q100 | 状态标签过滤绑定明细（非仅节点） | 已完成 |
| Q101 | 发布临时中转文件校验后清理 | 已完成 |
| Q102 | 发布成功后从确认清单移除已发配置 | 已完成 |
| Q106 | 发布历史/详情版本徽标可预览内容 | 已完成 |
| Q107 | 回滚页版本预览与绑定字段修正 | 已完成 |
| Q114 | 离线/未知节点禁止同步与发布/回滚 | 已完成 |
| Q147 | 发布未运行则 start；安装 listen 设置与非 root+80 告警 | 已完成 |
| Q150 | SSH/Nginx 双维度状态与门禁 | 已完成 |

### 任务中心

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q28–Q30 | 列表/详情布局与发布历史关联 | 已完成 |
| Q33–Q36 | 执行结果树与超链 | 已完成 |
| Q71 | 摘要与结果树协议统一 | 已完成 |
| Q105 | 同步结果树含跳过明细；任务详情默认展开 | 已完成 |
| Q109 | 协作式取消（断 SSH + 检查点） | 已完成 |
| Q110 | 筛选自动查询；表格定宽；操作 icon-only | 已完成 |
| Q111 | 列表无横向滚动；摘要窄列（全文见详情） | 已完成 |
| Q112 | 列表摘要主机最多展示 3 台 | 已完成 |
| Q115 | 详情失败原因按管道符还原多行 | 已完成 |
| Q148 | 升级/安装/启停摘要主行批次号；仪表盘副行截断对齐 | 已完成 |

### 节点 / 凭证 / 用户

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q43 | 详情内静默采集系统信息 | 已完成 |
| Q68 | 表单分区卡片与关联弹窗 | 已完成 |
| Q76 | 节点逻辑删除与同 IP 恢复 | 已完成 |
| Q77 | 批量导入/删除与文案 | 已完成 |
| Q78 | 用户/角色新增按钮 btn-sm | 已完成 |
| Q82 | 用户路由改 pk；用户名限 ASCII | 已完成 |
| Q83 | 用户可选所属用户组 | 已完成 |
| Q96 | data-* 去掉 escapejs，名称含 `-` 正常显示 | 已完成 |
| Q97 | 无关联节点启用凭证不创建测试任务 | 已完成 |
| Q98 | 密钥认证表单切换与私钥文件导入 | 已完成 |
| Q116 | 无权访问回首页 + showAlert | 已完成 |
| Q117 | 权限矩阵可勾选；全选按钮文案正确 | 已完成 |
| Q125 | 节点列表探测时间（上次探测成功） | 已完成 |
| Q126 | 周期性自动 SSH 测试不做 | 已关闭（结论） |
| Q150 | SSH/Nginx 双维度状态与门禁 | 已完成 |
| Q157 | 用户密码文案 / 侧栏 user 覆盖修复 / 安装启停卸载独立权限 | 已完成 |
| Q158 | 节点/凭证列表 xlsx 导出（凭证明文） | 已完成 |
| Q159 | 导出勾选/全量确认；凭证批量导入 | 已完成 |

### Nginx 升级 / 系统设置 / 导航 / 仪表盘

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q60–Q65 | 升级首页与升级中心向导迭代 | 已完成 |
| Q67 | 内置模块对齐官方 options | 已完成 |
| Q72–Q75 | 系统设置分组布局与接线 | 已完成 |
| Q73 | 侧栏可折叠 | 已完成 |
| Q81 | 升级设置生效时机修复 | 已完成 |
| Q108 | 首页最近任务改 TaskCenter；紧凑入口 | 已完成 |
| Q113 | 升级向导再进入从第 1 步空白开始 | 已完成 |
| Q116 | 首页快捷入口按权限收口 | 已完成 |
| Q118 | 源码包列表展示描述 | 已完成 |
| Q120 | 第三方模块在线复用 + 离线包双通道 | 已完成 |
| Q121 | 调整编译模块已编译项左侧可搜索 | 已完成 |
| Q122 | 已编译判定精确匹配（不含静态/dynamic 模糊） | 已完成 |
| Q123 | Nginx 升级任务保留天数（默认 90） | 已完成 |
| Q124 | 设置整数范围校验；包上限文案/默认 20MB | 已完成 |
| Q128 | 升级首页去掉冗余「上传源码包」入口 | 已完成 |
| Q129 | 升级首页去掉与 page_title 重复的卡片标题 | 已完成 |
| Q130 | 升级首页操作条改为内容区紧凑按钮行 | 已完成 |
| Q131 | 运维工具 Nginx 启停独立页 | 已完成 |
| Q132 | 运维工具 Nginx 全新安装独立页 | 已完成 |
| Q133 | 安装进源码包/模块包侧栏保持「安装」高亮 | 已完成 |
| Q134 | 安装历史对齐升级历史筛选能力 | 已完成 |
| Q135 | 启停独立历史页 | 已完成 |
| Q136 | 安装向导左右栏与确认对齐；安装管理三项缺省 | 已完成 |
| Q137 | 启停去冗余 hint + 最近启停任务 | 已完成 |
| Q138 | 升级/安装/启停最近条数复用 dashboard.recent_tasks_count | 已完成 |
| Q139 | 安装向导布局/第三方双通道/执行进度与日志页 | 已完成 |
| Q140 | 安装编译参数模块调整对齐升级弹窗 | 已完成 |
| Q141 | 安装开跑去掉全局进度遮罩 | 已完成 |
| Q142 | 安装进度保留展开态与失败原文同字号 | 已完成 |
| Q143 | 安装失败截断对齐升级；详情编译参数加 ./configure | 已完成 |
| Q144 | 安装批次 IN- 日自增；详情/最近任务链安装历史 | 已完成 |
| Q145 | 启停批次 OP-；历史/详情/最近任务按批展示跳转；升级最近任务补批次 | 已完成 |
| Q146 | 安装失败回填任务中心执行结果树 | 已完成 |
| Q147 | 发布未运行则 start；安装 listen 设置与非 root+80 告警 | 已完成 |
| Q148 | 升级/安装/启停摘要主行批次号；仪表盘副行截断对齐 | 已完成 |
| Q150 | SSH/Nginx 双维度状态与门禁（卸载后 orphaned） | 已完成 |
| Q152 | 运维工具 Nginx 卸载独立页 | 已完成 |
| Q153 | 安装/卸载按 systemd 管理能力分流 | 已完成 |
| Q154 | 卸载：包安装走 yum/apt remove | 已完成 |
| Q155 | 卸载探测并行 + 路径包含勾选锁定 | 已完成 |
| Q156 | 批量勾选上限统一 + 冗余并发设置下线 | 已完成 |
| Q157 | 用户密码文案 / 侧栏 user 覆盖修复 / 安装启停卸载独立权限 | 已完成 |

### UI 规范 / 性能 / 进度

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q22 | SSH 演进（并入 Q40） | 已关闭（不做） |
| Q38 | 进度弹窗扫描（并入 Q39） | 已关闭（不做） |
| Q39 | 假倒计时改为 TaskCenter 真实轮询 | 已完成 |
| Q40 | SSH 超时/并发配置化与异步采集 | 已完成 |
| Q41 | UI 规范总纲（16 类） | 已完成 |
| Q42 | UI 统一落地（126 项） | 已完成 |
| Q66 | data-table / 弹窗表 / 确认框统一 | 已完成 |
| Q69 | 全项目字号/字重对齐 | 已完成 |
| Q70 | 审计日志筛选与任务软链 | 已完成 |
| Q119 | 绑定/版本审计 detail 可读身份 | 已完成 |
| Q149 | UI 一致性收敛（弹窗尺寸/进度合一/主色桥接） | 已完成 |
| Q151 | Nginx 状态徽标短文案与发布/配置列表展示对齐 | 已完成 |
| Q152 | 运维工具 Nginx 卸载独立页 | 已完成 |
| Q153 | 安装/卸载按 systemd 管理能力分流 | 已完成 |
| Q154 | 卸载：包安装走 yum/apt remove | 已完成 |
| Q155 | 卸载探测并行 + 路径包含勾选锁定 | 已完成 |
| Q156 | 批量勾选上限统一 + 冗余并发设置下线 | 已完成 |
| Q157 | 用户密码文案 / 侧栏 user 覆盖修复 / 安装启停卸载独立权限 | 已完成 |
| Q158 | 节点/凭证列表 xlsx 导出（凭证明文） | 已完成 |
| Q159 | 导出勾选/全量确认；凭证批量导入 | 已完成 |

### 缺口与结论项（Q84–Q159）

| 编号 | 摘要 | 状态 |
|------|------|------|
| Q84 | 下线未写入的 `conflict` 过滤与汇总入口 | 已完成 |
| Q85 | 下线未写入的 `syncing` 过滤与汇总入口 | 已完成 |
| Q86 | 配置漂移检测不做；筛选隐藏类型 | 已关闭（结论） |
| Q87 | 任务类型下拉隐藏未实现项 | 已完成 |
| Q88 | 任务中心列表/详情权限对齐 | 已完成 |
| Q89 | 维持回滚=新任务；不使用 status=rollback | 已关闭（结论） |
| Q90 | 发布中心排除 `marked_deleted` | 已完成 |
| Q91 | 版本恢复一律标 `modified` | 已完成 |
| Q92 | Glob 预览仅单节点 | 已完成 |
| Q93 | 全局 running 门禁维持并文档化 | 已关闭（结论） |
| Q94 | 审计信号口径=落库成功变更 | 已关闭（结论） |
| Q95 | 删除遗留 ConfigVersion 与双版本路由；仅 bindings 版本入口 | 已完成 |
| Q96 | data-* 去掉 escapejs，名称含 `-` 正常显示 | 已完成 |
| Q97 | 无关联节点启用凭证不创建测试任务 | 已完成 |
| Q98 | 密钥认证表单切换与私钥文件导入 | 已完成 |
| Q99 | 配置列表「全部」状态标签默认可见 | 已完成 |
| Q100 | 状态标签过滤绑定明细（非仅节点） | 已完成 |
| Q101 | 发布临时中转文件校验后清理 | 已完成 |
| Q102 | 发布成功后从确认清单移除已发配置 | 已完成 |
| Q103 | 解除绑定文案/弹窗宽度与 marked_deleted 同步异常 | 已完成 |
| Q104 | 同步进度遮罩阶段与实时结果树 | 已完成 |
| Q105 | 同步结果树含跳过明细；任务详情默认展开 | 已完成 |
| Q106 | 发布历史/详情版本徽标可预览内容 | 已完成 |
| Q107 | 回滚页版本预览与绑定字段修正 | 已完成 |
| Q108 | 首页最近任务改 TaskCenter；紧凑入口 | 已完成 |
| Q109 | 协作式取消（断 SSH + 检查点） | 已完成 |
| Q110 | 筛选自动查询；表格定宽；操作 icon-only | 已完成 |
| Q111 | 列表无横向滚动；摘要窄列（全文见详情） | 已完成 |
| Q112 | 任务中心列表摘要主机最多展示 3 台 | 已完成 |
| Q113 | 升级向导再进入从第 1 步空白开始 | 已完成 |
| Q114 | 离线/未知节点禁止同步与发布/回滚 | 已完成 |
| Q115 | 任务详情失败原因多行格式化 | 已完成 |
| Q116 | 无权访问回首页 + showAlert；首页入口按权限 | 已完成 |
| Q117 | 权限矩阵可勾选；全选按钮文案正确 | 已完成 |
| Q118 | 源码包列表展示描述 | 已完成 |
| Q119 | 绑定/版本审计 detail 可读身份 | 已完成 |
| Q120 | 第三方模块在线复用 + 离线包双通道 | 已完成 |
| Q121 | 调整编译模块已编译项左侧可搜索 | 已完成 |
| Q122 | 已编译判定精确匹配（不含静态/dynamic 模糊） | 已完成 |
| Q123 | Nginx 升级任务保留天数（默认 90） | 已完成 |
| Q124 | 设置整数范围校验；包上限文案/默认 20MB | 已完成 |
| Q125 | 节点列表探测时间（上次探测成功） | 已完成 |
| Q126 | 周期性自动 SSH 测试不做 | 已关闭（结论） |
| Q127 | 周期性自动配置同步不做 | 已关闭（结论） |
| Q128 | 升级首页去掉冗余「上传源码包」入口 | 已完成 |
| Q129 | 升级首页去掉与 page_title 重复的卡片标题 | 已完成 |
| Q130 | 升级首页操作条改为内容区紧凑按钮行 | 已完成 |
| Q131 | 运维工具 Nginx 启停独立页 | 已完成 |
| Q132 | 运维工具 Nginx 全新安装独立页 | 已完成 |
| Q133 | 安装进源码包/模块包侧栏保持「安装」高亮 | 已完成 |
| Q134 | 安装历史对齐升级历史筛选能力 | 已完成 |
| Q135 | 启停独立历史页 | 已完成 |
| Q136 | 安装向导左右栏与确认对齐；安装管理三项缺省 | 已完成 |
| Q137 | 启停去冗余 hint + 最近启停任务 | 已完成 |
| Q138 | 升级/安装/启停最近条数复用 dashboard.recent_tasks_count | 已完成 |
| Q139 | 安装向导布局/第三方双通道/执行进度与日志页 | 已完成 |
| Q140 | 安装编译参数模块调整对齐升级弹窗 | 已完成 |
| Q141 | 安装开跑去掉全局进度遮罩 | 已完成 |
| Q142 | 安装进度保留展开态与失败原文同字号 | 已完成 |
| Q143 | 安装失败截断对齐升级；详情编译参数加 ./configure | 已完成 |
| Q144 | 安装批次 IN- 日自增；详情/最近任务链安装历史 | 已完成 |
| Q145 | 启停批次 OP-；历史/详情/最近任务按批展示跳转；升级最近任务补批次 | 已完成 |
| Q146 | 安装失败回填任务中心执行结果树 | 已完成 |
| Q147 | 发布未运行则 start；安装 listen 设置与非 root+80 告警 | 已完成 |
| Q148 | 升级/安装/启停摘要主行批次号；仪表盘副行截断对齐 | 已完成 |
| Q149 | UI 一致性收敛（弹窗尺寸/进度合一/主色桥接） | 已完成 |
| Q150 | SSH/Nginx 双维度状态与门禁（卸载后 orphaned） | 已完成 |
| Q151 | Nginx 状态徽标短文案与发布/配置列表展示对齐 | 已完成 |
| Q152 | 运维工具 Nginx 卸载独立页 | 已完成 |
| Q153 | 安装/卸载按 systemd 管理能力分流 | 已完成 |
| Q154 | 卸载：包安装走 yum/apt remove | 已完成 |
| Q155 | 卸载探测并行 + 路径包含勾选锁定 | 已完成 |
| Q156 | 批量勾选上限统一 + 冗余并发设置下线 | 已完成 |
| Q157 | 用户密码文案 / 侧栏 user 覆盖修复 / 安装启停卸载独立权限 | 已完成 |
| Q158 | 节点/凭证列表 xlsx 导出（凭证明文） | 已完成 |
| Q159 | 导出勾选/全量确认；凭证批量导入 | 已完成 |

---

## 正文

### Q1 · 配置列表 · 返回保持展开

- **问题**：节点展开后进入编辑/版本历史再返回列表，展开态丢失。
- **处理**：返回列表时恢复节点展开状态。
- **状态**：已完成

### Q2 · 配置列表 · 创建标签后无数据

- **问题**：手动添加配置标签后，列表看不到新建数据。
- **处理**：创建后跳转绑定创建页，config 预选。
- **状态**：已完成

### Q3 · 配置管理 · 绑定节点弹窗多选

- **问题**：绑定目标节点需支持弹窗多选，并按主机名/IP/节点组标签搜索。
- **处理**：`binding_create.html` 弹窗选择；`BindingCreateView` 循环创建多个绑定。
- **状态**：已完成

### Q4 · 配置管理 · 绑定弹窗与未绑定区

- **问题**：主机名粗体、行点击勾选、节点组搜索为空；需展示未绑定配置标签并可绑定/删除。
- **处理**：行点击勾选；API `groups__name` 匹配；列表增加未绑定标签区与删除弹窗。
- **状态**：已完成

### Q5 · 配置管理 · 未绑定标签不做批量

- **问题**：未绑定标签区是否需要批量操作与查询。
- **处理**：结论：不需要；辅助清理区逐个删除即可；创建后跳转由 Q2 覆盖。
- **状态**：已关闭（结论）

### Q6 · 配置列表 · 删除模板备注文案

- **问题**：手动添加中「可选：创建绑定时若远程无此文件…」备注可删。
- **处理**：删除 forms placeholder 与 models help_text，迁移 0004。
- **状态**：已完成

### Q7 · 配置管理 · 解除绑定文案与删除策略

- **问题**：远程无文件时仍提示「待删除/下次同步清理」。
- **处理**：`not_synced`/`orphaned` 直接物理删除；弹窗按状态区分文案。
- **状态**：已完成

### Q8 · 配置同步 · 失败标黄跳转

- **问题**：同步失败标黄应跳配置列表（主机名过滤）；列表状态应跳任务详情。
- **处理**：新增 `last_sync_task_id`；sync_wizard 标黄链列表；syncing/failed 链任务详情；单节点同步也建 TaskCenterTask。
- **状态**：已完成

### Q9 · 配置同步 · 单节点进度弹窗

- **问题**：单节点同步需动态进度。
- **处理**：异步线程 + TaskCenterTask；`ConfigSyncProgressView` 读真实进度；前端按 `task_center_id` 轮询。进度 UI 最终由 Q39 统一为全屏遮罩。
- **状态**：已完成

### Q10 · 配置同步 · 读秒进度

- **问题**：多配置串行时无法按配置数展示真实百分比。
- **处理**：曾用 `setInterval` 读秒作为临时方案；最终由 Q39 改为 TaskCenter 真实轮询。
- **状态**：已完成
- **关联**：被 Q39 取代

### Q11 · 配置管理 · 双入口手动添加

- **问题**：同步页按节点「手动添加」应限定该节点，不应跳通用绑定创建。
- **处理**：有 `node_id` 时自动创建绑定并回列表；无则按 Q2 跳绑定页。
- **状态**：已完成

### Q12 · 发布管理 · 中心/任务/历史重设计

- **问题**：发布中心、任务中心、发布历史需按节点/配置关联重构交互与导航（对齐 [`docs/08-releases.md`](docs/08-releases.md)）。
- **处理**：
  1. 发布中心：节点二维 2 步选择 + API；快捷推送；单条重试；进度按节点分组。
  2. 任务中心/历史：树形/Accordion；侧栏「发布历史」。
  3. 发布粒度最终由 Q21 定为全量/节点/单配置三级按钮（不再使用顺序/并行开关）。
- **状态**：已完成

### Q13 · 发布中心 · 选择区交互与表格视觉

- **问题**：已选计数位置、清除对齐、快捷列、全选、路径预览、表头/表行字重字号需统一。
- **处理**：计数迁分页行；清除对齐筛选行；删快捷列；表头全选；路径 Modal 预览；表格字体/背景微调。
- **状态**：已完成

### Q14 · 发布中心 · 勾选节点未全选绑定

- **问题**：勾选节点后绑定配置未全部勾选。
- **处理**：去掉仅 modified 限制；已展开反向联动；未展开异步加载并填 `selectedBindings`。
- **状态**：已完成

### Q15 · 发布中心 · 列宽与节点单行展示

- **问题**：表头列宽失衡；状态换行；节点需 `hostname(IP) + 组 badge` 单行。
- **处理**：状态列加宽 nowrap；`.node-info-cell` / `.node-identity`。
- **状态**：已完成

### Q16 · 发布中心 · 绑定数与行点击展开

- **问题**：绑定数只显示总数；行点击应展开明细。
- **处理**：仅总数 badge；勾选时自动展开绑定。
- **状态**：已完成

### Q17 · 发布中心 · 配置预览网络错误

- **问题**：路径预览请求失败。
- **处理**：URL 改为 `/releases/version/{id}/content/`；适配扁平 JSON。
- **状态**：已完成

### Q18 · 发布中心 · 绑定行点击勾选

- **问题**：点击配置明细行应勾选。
- **处理**：`.binding-item` 行点击切换复选框（排除 INPUT/SELECT/BUTTON）。
- **状态**：已完成

### Q19 · 发布中心 · 预览与版本不匹配

- **问题**：切换历史版本后预览仍显示最新内容。
- **处理**：从 DOM 读版本下拉当前值，匹配 versions 取对应 ID；徽标同步版本号。
- **状态**：已完成

### Q20 · 发布中心 · 状态过滤栏

- **问题**：需类似配置列表的状态快速过滤。
- **处理**：API `sync_status` + `status_counts`；前端状态标签过滤栏。绑定过滤最终口径见 Q90（排除 `marked_deleted`）。
- **状态**：已完成

### Q21 · 发布中心 · 确认清单与发布粒度

- **问题**：节点名 fallback 错误；清单需按节点折叠并支持搜索；发布粒度改为全量/节点/单配置。
- **处理**：Accordion 清单；`publishBindings()` 三级按钮；tag 搜索；节点名从 DOM 读取。
- **状态**：已完成

### Q22 · 性能 · SSH 通信演进

- **问题**：是否按早期方案单独演进 SSH 层。
- **处理**：结论：本条不单独实施，并入 Q40。
- **状态**：已关闭（不做）
- **关联**：父任务 Q40

### Q23 · 发布中心 · 确认清单表格与查询

- **问题**：「发布此节点」换行、单配置无 title、行点击不折叠、查询非 tag 且无效。
- **处理**：按钮 nowrap；title 提示；整行 toggle；tag-input + AND 多标签过滤。
- **状态**：已完成

### Q24 · 发布中心 · 需点两次才展开

- **问题**：节点行有时需点击两次才展开绑定。
- **处理**：展开态去掉 `d-none`，避免 Bootstrap `!important` 覆盖内联 display。
- **状态**：已完成

### Q25 · 发布中心 · 查询与节点展示

- **问题**：查询无效；主机名粗体；Step2 表头/行信息不全。
- **处理**：API 增配置名搜索；去 strong；表头与 IP/组展示补齐。
- **状态**：已完成

### Q26 · 发布中心 · Step2 表头与节点行

- **问题**：表头宜为「配置名称」；节点行缺 IP/组；去掉「N 个配置」。
- **处理**：表头调整；预选 Promise 链；DOM fallback 补 IP/组；去掉数量 badge。
- **状态**：已完成

### Q27 · 发布中心 · 删除传统创建方式

- **问题**：删除传统发布页且不影响 JSON 发布 API。
- **处理**：`ReleaseCreateAPIView` 仅 JSON；路由 `api/create/`；删除传统 create 模板/forms；测试适配。
- **状态**：已完成

### Q28 · 任务中心 · 列表布局

- **问题**：摘要列空白过大、列换行；左侧展开按钮可移除。
- **处理**：摘要定宽；td nowrap；删展开列改行点击展开（行内展开最终由 Q29 移除）。
- **状态**：已完成

### Q29 · 任务中心 · 去掉行内展开

- **问题**：有发布历史入口后，任务行内展开详情是否必要。
- **处理**：结论：删除 expand 模板/CSS/JS；保留详情按钮与进度轮询。
- **状态**：已完成

### Q30 · 任务中心 · 详情字体与发布历史关联

- **问题**：详情字重不统一；需关联发布历史。
- **处理**：统一 `.task-detail-body` 字号；发布类批次链发布历史；非发布类不显示额外按钮。
- **状态**：已完成

### Q31 · 发布中心 · 刷新后「加载绑定…」

- **问题**：刷新后偶发绑定加载失败。
- **处理**：`loadNodes` 后对已展开且无缓存节点调用 `loadBindings`（sessionStorage 只恢复展开态）。
- **状态**：已完成

### Q32 · 配置列表 · 节点展示对齐

- **问题**：节点展示需对齐发布中心；去掉快速推送。
- **处理**：`.node-info-cell` 结构；删推送按钮与旧组标签 CSS。
- **状态**：已完成

### Q33 · 任务中心 · 执行结果优化

- **问题**：详情执行结果难定位失败项。
- **处理**：失败置顶并默认展开；成功折叠；总耗时；结果卡片化；失败行浅红底。原始日志区由 Q35 删除。
- **状态**：已完成

### Q34 · 任务中心 · 结果计数错误

- **问题**：summary 计数为 0；配置名带版本后缀。
- **处理**：按前导空格匹配 `[成功]`/`[失败]`；配置名去掉版本后缀；删除重复 detail alert。
- **状态**：已完成

### Q35 · 任务中心 · 删原始日志 + 配置超链

- **问题**：原始日志区可删；配置名应新窗口跳发布历史。
- **处理**：删 details 日志区；配置 `<a target="_blank">`；search 多词 + batch/node_ip 过滤；有过滤时自动展开历史树。
- **状态**：已完成

### Q36 · 任务中心 · 批次号新窗口

- **问题**：来源批次需新窗口打开。
- **处理**：批次超链加 `target="_blank"`。
- **状态**：已完成

### Q37 · 发布历史 · 详情导航按钮

- **问题**：「返回发布中心」失效；「任务中心」实际返回上页。
- **处理**：改为「返回发布历史」；任务中心纯链接新窗口；`goBackToReleaseHistory` fallback 修正为 `releases:list`。
- **状态**：已完成

### Q38 · 整体 · 进度弹窗扫描

- **问题**：是否单独扫描并统一读秒/进度弹窗风格。
- **处理**：结论：本条不单独实施，并入 Q39。
- **状态**：已关闭（不做）
- **关联**：父任务 Q39

### Q39 · 进度弹窗 · 假倒计时改真实轮询

- **问题**：多处假倒计时；进度风格不统一。
- **处理**：
  1. `base.html` 全局 `#asyncProgressOverlay`；完整日志链任务详情。
  2. 节点 SSH 测/解锁、凭证启用、同步批/单节点改 TaskCenter 轮询。
  3. 删除假 setInterval 读秒；任务中心表格/升级面板保持自有真实进度。
- **状态**：已完成
- **关联**：承接 Q38；取代 Q10 读秒

### Q40 · SSH · 配置化与异步化

- **问题**：超时/并发硬编码；系统信息/Nginx 版本同步阻塞 HTTP。
- **处理**：
  1. `get_setting` 接线：`ssh_connect_timeout`、`batch_max_count` 等（凭证/同步/发布独立并发键后由 Q156 并入 `node.batch_max_count`）。
  2. 系统信息/Nginx 版本改为异步 TaskCenter；列表轮询更新 DOM。
  3. 结论：不在本条实施 asyncio 层。
- **状态**：已完成
- **关联**：承接 Q22

### Q41 · UI 规范 · 总纲

- **问题**：全项目 UI 需统一规范。
- **处理**：确立 16 类规范（字体/颜色/布局/按钮/表格/分页/弹窗/表单/图标/反馈/交互/数据展示/代码/响应式/查询/其他）及落地原则：复用公共组件、不改业务逻辑、禁止硬编码主题值。细则由 Q42 落地。
- **状态**：已完成
- **关联**：子任务 Q42

### Q42 · UI 统一 · 126 项落地

- **问题**：按 Q41 审计落地全项目 UI（含二次核实）。
- **处理**（共 126 项）：
  1. **全局样式**：CSS 变量、tag-input 全局化、`.data-table`、字号 rem、圆角/过渡两档、等宽 `.code-font`。
  2. **全局组件**：empty-state、pagination（首页末页+每页条数）、btn-sm、showConfirm、状态/环境 badge、form-switch、Toast、必填 `*`、Emoji→BI。
  3. **登录/个人/仪表盘**：变量对齐、去嵌套 container、统计卡圆角。
  4. **业务页**：nodes/credentials/configs/releases/upgrade/users/audit/settings 分页、空态、表格、表单对齐。
  5. **导航/错误页**：侧栏图标箭头、404/500/403。
  6. **二次核实**：perm-matrix 全局、删死代码（syncProgressModal 等）、弹窗 ID 前缀、删除页文案统一等。
- **状态**：已完成
- **关联**：父规范 Q41

### Q43 · 节点列表 · 详情静默采集

- **问题**：点主机名先出「系统信息采集」进度，再出详情。
- **处理**：详情弹窗内静默加载系统信息/Nginx，去掉全屏进度遮罩。
- **状态**：已完成

### Q44 · 配置管理 · 返回版本历史 404

- **问题**：对比页返回链到 `configs/<id>/versions/`，误把 config.id 当 binding。
- **处理**：改为 `configs:binding_versions` + binding.id；配置标签详情去掉误用版本历史按钮。
- **状态**：已完成

### Q45 · 发布中心 · 完整日志按批次

- **问题**：完整日志进任务中心展示全部，应只看当前批次。
- **处理**：跳转 `/releases/history/?search=<批次号>`（按批次过滤展示）。
- **状态**：已完成

### Q46 · 发布中心 · 搜索后卡在加载绑定

- **问题**：条件搜索后明细显示「加载绑定…」，需刷新才恢复。
- **处理**：恢复展开时有 `bindingCache` 则直接 `renderBindingRow`，无缓存才 `loadBindings`。
- **状态**：已完成

### Q47 · 发布中心 · 配置名搜索无反应

- **问题**：配置名称标签搜索无效。
- **处理**：API 增 `remote_path` 搜索；有搜索词自动展开本页；绑定行按名/路径过滤。
- **状态**：已完成

### Q48 · 发布中心 · 首发文件备份失败

- **问题**：远程无文件时 `cp` 备份失败。
- **处理**：远程不存在则跳过备份；失败回滚改为 `rm` 清理新文件。
- **状态**：已完成

### Q49 · 发布历史 · 回滚版本预览失败

- **问题**：版本号弹窗「加载失败」。
- **处理**：适配 VersionContentAPIView 扁平 JSON（同 Q17）。
- **状态**：已完成

### Q50 · 发布历史 · 回滚后 pending 无进度

- **问题**：回滚变为 pending 但无执行反馈。
- **处理**：确认后异步执行 + 发布同款 `#asyncProgressOverlay`；完整日志按批次打开。
- **状态**：已完成

### Q51 · 发布历史 · 失败是否可回滚

- **问题**：失败发布是否提供回滚入口。
- **处理**：success|failed 均可回滚（失败作自动还原兜底）；前后端校验统一。
- **状态**：已完成

### Q52 · 发布历史 · 批量回滚改勾选

- **问题**：批次头挂批量回滚操作路径长。
- **处理**：顶部操作栏 + 勾选；`api/selected-rollback`；支持批次/节点全选。
- **状态**：已完成

### Q53 · 发布历史 · 勾选框过大

- **问题**：form-check-input 随字号放大。
- **处理**：改原生 checkbox，对齐节点列表。
- **状态**：已完成

### Q54 · 发布历史 · 分页与表格结构

- **问题**：按配置分页导致跨节点割裂；勾选密度高、对齐差。
- **处理**：曾按 `(batch, node)` 单元分页；data-table 三级行；勾选仅表头+配置行。最终分页口径由 Q56 改为按批次。
- **状态**：已完成

### Q55 · 发布历史 · 状态与批次汇总

- **问题**：状态未对齐；「N 任务」可去掉；节点行状态可上收到批次。
- **处理**：状态 nowrap；删任务数；批次汇总成功/部分失败/全部失败。
- **状态**：已完成

### Q56 · 发布历史 · 按批次分页与底栏

- **问题**：部分失败未对齐；应按批次分页；分页靠右下。
- **处理**：批次行拆列状态；按 `batch_number` 分页；底栏对齐节点列表。
- **状态**：已完成

### Q57 · 发布历史 · 多粒度勾选回滚

- **问题**：需支持单/多节点、单/多配置回滚勾选。
- **处理**：节点/配置/表头三级联动 + indeterminate；仍按 task_ids 调 API。
- **状态**：已完成

### Q58 · 发布历史 · 批量回滚版本策略

- **问题**：批量无法选版，是否保留批量回滚。
- **处理**：结论：保留；默认回滚各任务 publish_version 上一版；跨批次同 binding 仅保留最新任务；精细选版走单配置回滚。
- **状态**：已完成

### Q59 · 发布历史 · 批量回滚明细弹窗

- **问题**：确认框 HTML 原文异常；需展示回滚明细与版本。
- **处理**：modal-lg 按节点分组展示「当前→回滚至」；binding 去重对齐后端。
- **状态**：已完成

### Q60 · Nginx 升级 · 首页运维台

- **问题**：升级首页信息密度过高，不利于运维扫读。
- **处理**：顶栏快捷入口 + 可点统计卡 + 最近任务表；历史支持 running 过滤。
- **状态**：已完成

### Q61 · 发布 · 备份按主机分子目录

- **问题**：备份平铺易同名覆盖。
- **处理**：`{backup_dir}/{hostname}/filename.timestamp`；接通系统设置；hostname 路径安全处理。
- **状态**：已完成

### Q62 · Nginx 升级 · 源码包覆盖与上传进度

- **问题**：同版本 IntegrityError；需真实上传进度。
- **处理**：预检 + showConfirm 覆盖；overwrite 更新；XHR `upload.onprogress` + asyncProgressOverlay。
- **状态**：已完成

### Q63 · Nginx 升级 · 真实编译错误与当前版本

- **问题**：页面错误被 dpkg 失败掩盖；只显示目标版本。
- **处理**：按 exit code 合并 stdout/stderr；预检仅 gcc/make；先写 current_version；版本列纯版本号。
- **状态**：已完成

### Q64 · Nginx 升级 · 升级中心向导

- **问题**：单页同时承载节点/源码包/高级项，需拆分并向导化；需多选节点。
- **处理**：
  1. 四步向导：选节点+包 → 编译环境 → 编译参数 → 确认。
  2. 节点弹窗多选 + tag 搜索；源码包 data-table；高级项折叠取系统设置。
  3. `batch_number` + 并行升级 + batch-progress；参数高亮渲染。
- **状态**：已完成

### Q65 · Nginx 升级 · 中心深度迭代

- **问题**：源码包查询、多节点参数一致性、模块对比、进度/详情、configure 引号截断、reload/PID、二次确认等需完善。
- **处理**（按主题）：
  1. **源码包/参数一致**：tag 过滤；全量 params 签名；差异弹窗可展开；删除不准确文案。
  2. **模块 UI**：modal-xl 左「已编译」右「目标已选」+ 添加区；官方模块见 Q67。
  3. **确认/进度/详情**：最终参数批注；删重复展示与堆叠错误；日志 strip + 自动滚底；task_log 2s 轮询。
  4. **状态保持/确认**：sessionStorage；showConfirm 排队；开跑后按钮仍可点并警告新批次。
  5. **configure 拼装**：shlex/引号感知分词 + 安全 join，避免 `--with-cc-opt='...'` 截断。
  6. **安装与 reload**：make install → nginx -t → detect+reload（`utils/nginx_ops.py`）；成功回写节点版本；PID 用 pid-path。
- **状态**：已完成

### Q66 · 排版 · data-table 与弹窗统一

- **问题**：多页表格首列未对齐；弹窗表未统一；行勾选不全；确认风格不一。
- **处理**：全局 data-table 首末列留白；node-info 提升全局；弹窗 picker 表 + `bindModalTableRowToggle`；showConfirm/showAlert 收敛。
- **状态**：已完成

### Q67 · Nginx 升级 · 补全官方模块参数

- **问题**：左侧可选模块过少。
- **处理**：`BUILTIN_ADD_MODULES` 按官方 configure/auto/options 补全；静态与 dynamic 互斥。
- **状态**：已完成

### Q68 · 用户/凭证/节点 · 表单分区与关联弹窗

- **问题**：用户操作列换行；编辑页分区不足；关联角色/组需弹窗；凭证眼睛错位；各模块表单风格不一。
- **处理**：
  1. 用户操作 icon-only；编辑分区卡 + 角色弹窗 + perm-matrix；列表加所属用户组列。
  2. 角色/用户组/凭证/节点/节点组统一 `.form-section` 色条分区卡。
  3. 凭证眼睛相对输入框垂直居中。
- **状态**：已完成

### Q69 · 字体 · 全项目字号字重

- **问题**：配置列表与发布中心主机名字号不一致等。
- **处理**：表头 600；`.node-identity` 固定 base/400；清页面级 badge 字号；表行去 strong。
- **状态**：已完成

### Q70 · 审计 · 筛选与任务软链

- **问题**：查询布局、类型筛选、时间快捷、异步任务关联不足。
- **处理**：AuditLog 增 `task_center_id`/`source_batch`；结果筛选 + 今天/7/30 天；详情「查看任务」；登录日志同步优化。
- **状态**：已完成

### Q71 · 任务中心 · 摘要与结果树协议

- **问题**：摘要过长/缺目标；非发布类型结果不规范。
- **处理**：`task_result.py` 统一树协议；各类型 finish 写树；列表 `format_task_center_summary`；升级/凭证等详情入口区分。
- **状态**：已完成

### Q72 · 系统设置 · 分组布局

- **问题**：设置页信息密度高，缺少分组导航。
- **处理**：左分组导航 + 右 side-by-side 字段；未保存离开确认；`?group=` + localStorage；原型见 `docs/prototypes/settings-gitlab-prototype.html`。
- **状态**：已完成

### Q73 · 导航 · 侧栏可折叠

- **问题**：侧栏偏宽，需可伸缩。
- **处理**：app-shell 220px/64px；localStorage；折叠浮层子菜单。
- **状态**：已完成

### Q74 · 系统设置 · 全宽左右布局

- **问题**：设置页居中导致可用宽度不足。
- **处理**：去 max-width；左 240px / 右 flex:1；窄屏一级横滑。
- **状态**：已完成

### Q75 · 系统设置 · 所见即所得 + 保留天数

- **问题**：多项设置未生效；缺数据保留天数。
- **处理**：PRESET 仅保留已接线项并启动 seed；补齐仪表盘/SSH/同步/发布/上传/轮询等接线；新增 4 类保留天数 + 每日 purge（跳过 pending/running）。
- **状态**：已完成

### Q76 · 节点 · 逻辑删除

- **问题**：物理删导致发布历史消失；同 IP 再添加应关联历史。
- **处理**：`is_deleted` + ActiveNodeManager；历史保留并标「已删除」；同 IP 恢复原主键；已删禁用回滚勾选。
- **状态**：已完成

### Q77 · 节点列表 · 批量导入/删除与文案

- **问题**：批量按钮带数量；「添加」改「新增」；需 Excel 批量导入。
- **处理**：xlsx 模板+整文件校验；默认环境/路径取系统设置；同 IP 走恢复；批量逻辑删除；主配置路径列。
- **状态**：已完成

### Q78 · 用户管理 · 新增按钮尺寸

- **问题**：用户/角色「新增」比用户组略大。
- **处理**：补 `btn-sm` 对齐。
- **状态**：已完成

### Q79 · 发布历史 · 详情元信息布局

- **问题**：详情字段竖排，扫描效率低。
- **处理**：对齐任务详情 `task-detail-body` 同行标签；状态迁入元信息行。
- **状态**：已完成

### Q80 · 发布 · 性能与进度展示

- **问题**：发布过程反馈不足；每配置 reload 过频；进度弹窗含详细 SSH 折叠。
- **处理**：
  1. 同节点复用 SSH；`log_output` 增量；进度展示当前步骤 + 结果树。
  2. 同节点批次：备份→上传→nginx -t，统一 reload 一次；失败回滚本批已上传。
  3. 弹窗去掉详细 SSH 折叠，保留完整日志跳转；统一步骤文案。
- **状态**：已完成

### Q81 · 系统设置 · 升级默认值生效

- **问题**：改工作目录/-j 后升级中心仍用旧值。
- **处理**：sessionStorage 按 `settingsBaseline` 智能恢复；创建/make/mkdir 空值回退 `get_setting`；PRESET description 补生效时机。
- **状态**：已完成

### Q82 · 用户列表 · 中文用户名 NoReverseMatch

- **问题**：非法用户名导致列表 `NoReverseMatch`。
- **处理**：edit/delete/lock 路由改 `<int:pk>`；表单限制 `[-a-zA-Z0-9_]+`，中文填姓名。
- **状态**：已完成

### Q83 · 用户管理 · 所属用户组

- **问题**：创建/编辑用户应可选用户组（对齐节点关联节点组）。
- **处理**：表单 `teams` 字段；分区 + 弹窗多选 chips；View 传入 `all_user_teams`。
- **状态**：已完成

### Q84 · 配置管理 · conflict 状态无写入

- **问题**：`sync_status=conflict` 在模型与多处 UI 存在但从未赋值，过滤/汇总恒空。
- **处理**：下线配置列表过滤、节点汇总徽章入口、发布中心过滤、仪表盘冲突卡片；model choices 保留防脏数据；行内 badge 兜底仍可显示。
- **状态**：已完成

### Q85 · 配置管理 · syncing 状态无写入

- **问题**：异步同步不置 `syncing`，过滤标签恒空。
- **处理**：与 Q84 一并下线主推过滤/汇总；不实现过程态写入。
- **状态**：已完成

### Q86 · 配置管理 · 漂移检测未实现

- **问题**：hash/漂移字段与任务类型预留，无巡检实现。
- **处理**：结论：不做漂移巡检；任务类型筛选隐藏 `config_drift_check`；字段保留。
- **状态**：已关闭（结论）

### Q87 · 任务中心 · 操作类型与实现不一致

- **问题**：枚举含 discover/glob/drift，实际发现走 `config_batch_sync`，Glob 不建任务。
- **处理**：任务中心类型下拉隐藏未用三项；同步创建处注释标明统一类型；审计 MAP 保留历史展示兼容。
- **状态**：已完成

### Q88 · 任务中心 · 列表/详情权限不对称

- **问题**：仅 `nodes.update` 时列表与详情可见类型不一致。
- **处理**：列表与详情统一为本人 `node_batch_test` + `config_batch_sync`。
- **状态**：已完成

### Q89 · 发布历史 · rollback 状态未使用

- **问题**：choices 含 `rollback`，回滚实为新建任务。
- **处理**：结论：维持「回滚=新任务」模型；不改枚举、不回写源任务 status。
- **状态**：已关闭（结论）

### Q90 · 发布中心 · 绑定含 marked_deleted

- **问题**：待删绑定仍可出现在发布勾选。
- **处理**：绑定 API 与状态统计排除 `marked_deleted`；发布中心去掉「标记删除」过滤标签。
- **状态**：已完成

### Q91 · 配置版本 · 恢复直接标 synced

- **问题**：恢复到曾同步版本时直接标 `synced`。
- **处理**：恢复后一律 `modified`，提示需重新发布。
- **状态**：已完成

### Q92 · 配置 Glob 预览 · 多节点只取 first

- **问题**：多 `node_ids` 仅处理第一个。
- **处理**：多于 1 个节点返回 400；仅支持单节点预览。
- **状态**：已完成

### Q93 · 发布 · 全局 running 阻断新批次

- **问题**：任意 running 发布即挡新批次。
- **处理**：结论：任意 `ReleaseTask.status=running` 时拒绝自动启动新发布批次；该约束已写入需求文档。
- **状态**：已关闭（结论）

### Q94 · 审计 · CRUD 信号结果多为 success

- **问题**：信号写审计多为 success。
- **处理**：结论：模型落库成功变更记 success；不扩展覆盖全部业务失败路径。
- **状态**：已关闭（结论）

### Q95 · 配置管理 · 遗留 ConfigVersion 与双版本路由

- **问题**：旧模型与兼容路由并存；`/configs/<pk>/versions/` 易把 config.id 当 binding（Q44）。
- **处理**：
  1. 删除遗留 URL（`versions`/`version_detail`/`restore`/`version_compare*`），仅保留 `/configs/bindings/<pk>/versions/…` 与 compare。
  2. 删除 `ConfigVersion` 模型 + 迁移 `0002_delete_configversion`（库中无有效数据，不做内容迁移）。
  3. 审计 TRACKED_MODELS 去掉 ConfigVersion；`base.html` navFallbackMap 改为 `binding_*` 正式路由名。
- **状态**：已完成

### Q96 · 凭证/用户/节点组 · data-* 误用 escapejs

- **问题**：名称含 `-`（如 `cmas-mas`）时，停用/启用/删除弹窗显示 `cmas\u002Dmas`。
- **处理**：经 `getAttribute` 读取的 `data-*` 去掉 `|escapejs`，改用默认 HTML 转义（凭证 `data-credential-name`、用户 `data-username`、节点组 `data-group-name`）；JS 字符串字面量（如 `onclick='...|escapejs'`）仍保留 `escapejs`。
- **状态**：已完成

### Q97 · 凭证管理 · 无关联节点启用不走测试

- **问题**：0 台关联节点时启用仍弹「启动测试任务」，并创建空测试任务/进度遮罩。
- **处理**：前端按 `data-node-count` 分支：无节点用「确认启用」文案；后端无关联节点仅启用、不建 TaskCenter/CredentialEnableTask。
- **状态**：已完成

### Q98 · 凭证管理 · 密钥认证切换与私钥导入

- **问题**：创建页选「密钥认证」仍显示密码区；密钥认证仅能粘贴。
- **处理**：
  1. 凭证 `create.html` 去掉对 RadioSelect 不存在的 `id_auth_type` 依赖，对齐 `edit.html` 按 radio `change` 切换 `#passwordField` / `#privateKeyField`。
  2. 创建/编辑页增加「选择私钥文件」：`FileReader` 读入 textarea；前端拒空文件与加密私钥（PEM ENCRYPTED / Proc-Type / OpenSSH cipher≠none）；格式仍由服务端 paramiko 校验。
  3. 结论：不做加密私钥口令解密；不新增后端上传接口。
- **状态**：已完成

### Q99 · 配置列表 · 「全部」状态标签默认不可见

- **问题**：默认选中「全部」时白字无背景，贴白底近似不可见。
- **处理**：`.config-status-active` 补默认灰底（`#6c757d`），对齐发布中心；各状态专用色规则仍覆盖。
- **状态**：已完成

### Q100 · 发布中心 · 状态标签明细混入其它状态

- **问题**：状态标签只过滤「含该状态绑定」的节点；展开/勾选仍用节点下全部绑定，导致明细混入其它状态。
- **处理**：前端 `matchSyncStatusFilter` / `filterBindingsBySyncStatus`（pending=`not_synced|modified`）；`renderBindingRow` 展示层过滤；`populateBindingsFromCache` / `fetchAndPopulateBindings` 勾选只纳入匹配项；切换标签时 `pruneSelectedBySyncStatus` 收缩已选。
- **状态**：已完成

### Q101 · 发布 · 临时中转文件残留

- **问题**：上传中转 `/tmp/*.mngxops_tmp` 校验后未删除。
- **处理**：临时路径改为 `/tmp/{filename}.mngxops_tmp.{task.id}`；复制+MD5 通过后 `remove_remote_file` 清理；失败路径尽力清理；日志区分「备份」与「临时中转文件」。
- **状态**：已完成

### Q102 · 发布中心 · 成功后确认清单仍展示已发配置

- **问题**：发布任务成功后 Step2「确认发布清单」仍保留已选绑定。
- **处理**：`publishBindings` 记录 `lastPublishedBindingIds`；进度轮询 `finished && status===success` 时 `removePublishedFromSelection` 剔除、同步 Step1 勾选、重建预览；清单空则回 Step1。部分失败时保留清单便于重试。
- **状态**：已完成

### Q103 · 配置管理 · 解除绑定文案与 marked_deleted 同步卡住

- **问题**：
  1. 解除绑定确认文案写「待删除」，状态实际为「已标记删除」。
  2. 确认弹窗默认 `modal-sm` 过窄。
  3. 标记删除后再同步：`_ensure_binding` 排除 `marked_deleted` 后仍 create，触发 `(config,node)` unique 冲突；线程未捕获异常，TaskCenter 停在 `running`。
- **处理**：
  1. `list.html` / `binding_delete.html` 文案改为「已标记删除」；`showConfirm` 传 `md`。
  2. `_ensure_binding` 遇 `marked_deleted` 跳过导入，末尾仍 `_cleanup_marked_deleted_bindings`；无发现结果时也执行清理。
  3. 单/批同步线程加 try/except，异常时任务标 `failed`。
  4. 清理返回已删配置名；同步完成 `detail` 为「N 新增, M 更新, D 删除」，结果树含 `xxx (删除)`。
- **状态**：已完成

### Q104 · 配置同步 · 进度遮罩动态阶段与结果树

- **问题**：单/批同步遮罩仅一行 `detail`，无阶段与实时结果树；进度 API 无 `releases.read` 时不返回 `config_batch_sync`。
- **处理**：
  1. `TaskCenterProgressAPIView` 放行本人 `config_batch_sync`（及 `configs.update` 可访问进度接口）。
  2. 全局 `#asyncProgressOverlay` 渲染 `current_steps` + 结果树（对齐发布）。
  3. 同步复用 `_set_current_step`：连接远程 → 发现配置 → 新建/更新/跳过/清理删除 · name；进行中刷活树，结束写最终树。
- **状态**：已完成

### Q105 · 任务中心 · 同步结果配置明细

- **问题**：同步详情仅见汇总；全跳过时结果树无配置名；成功节点默认折叠。
- **处理**：
  1. 批量/单节点最终结果树写入 `新建`/`更新`/`删除`/`跳过` 逐条明细（`skipped_names`）；进行中活树仍不落跳过行。
  2. `config_batch_sync` 任务详情默认展开结果树，并展示 `task.detail` 摘要。
- **状态**：已完成

### Q106 · 发布历史 · 版本徽标内容预览

- **问题**：发布历史/详情版本 badge 不可点，无法查看该版本配置正文。
- **处理**：
  1. 列表 `V{n}`、详情元信息与操作记录版本改为可点；`data-version-id` 取 `ReleaseTask.version_id` 或按 binding+版本号解析。
  2. 复用回滚页 `#versionPreviewModal`，`GET /releases/version/<id>/content/` 展示对应版本内容。
- **状态**：已完成

### Q107 · 回滚配置 · 页头版本预览与字段修正

- **问题**：回滚页页头「当前版本」徽章不可点；表格高亮误用 `config.current_version`、路径误用 `config.file_path`（二者属 binding）；点击版本号无法弹出配置预览。
- **处理**：
  1. 页头版本对齐详情可点预览（`version-preview-link` + `data-version-id`）；路径改 `task.remote_path` / binding 回退。
  2. 表格「当前版本」对比改为 `task.binding.current_version`。
  3. 版本预览 JS 对齐发布历史 list.html（独立 `<script>`、`preventDefault`+`stopPropagation`、同弹窗同 API）；修复 `buildNodeGroup` 三元表达式缺 `)` 导致整页脚本无法解析的问题。
  4. `ReleaseRollbackView.get` 补 `select_related("version")`。
- **状态**：已完成

### Q108 · 仪表盘 · 首页改版

- **问题**：「配置下发失败」与任务中心入口不一致；「最近发布任务」仅 `ReleaseTask`；快捷操作卡片过大；缺执行中/失败等运维信号。
- **处理**：
  1. 删除失败绑定告警表；下线设置项 `dashboard.recent_failed_bindings_count`。
  2. 「最近任务」改查 `TaskCenterTask`（含类型列与摘要，权限对齐任务中心）；`dashboard.recent_tasks_count` 默认 20。
  3. 页头改为紧凑 `btn-sm`（发布中心/配置列表/新建节点/任务中心）。
  4. 统计卡改为：节点总数/在线/离线/待推送/执行中任务/近 7 天失败；`/api/stats/` 同步返回。
  5. 原型：`docs/prototypes/dashboard-home-prototype.html`。
- **状态**：已完成

### Q109 · 任务中心 · 协作式取消

- **问题**：部分任务长期「执行中」无取消入口；执行线程不读 `cancelled`。
- **处理**：
  1. 列表/详情对 `pending`/`running` 增加「取消」；`showConfirm` 说明状态、断 SSH、远程可能继续。
  2. `POST /releases/tasks/<pk>/cancel/`：标 `cancelled`、级联同批次 `ReleaseTask` 与关联 `NginxUpgradeTask`、关闭已登记 SSH。
  3. `task_cancel.py`：`is_cancelled` / SSH 登记 / `finish_if_active`；发布/同步/升级/节点/凭证接入检查点与终态守卫。
  4. 结论：不做远程/本地 `kill -9`；断连后远程进程可能残留。
- **状态**：已完成

### Q110 · 任务中心 · 列表筛选与操作列

- **问题**：类型/状态下拉切到「全部」不自动查询；表格过宽需横向拖动；操作列带文字，与其它列表 icon-only 不一致。
- **处理**：
  1. `operation_type`/`status` 增加 `onchange` 提交表单。
  2. `table-layout: fixed`；去掉整表 `nowrap` 与摘要横向滚动，摘要单行省略 + `title` 悬停。
  3. 操作列 `btn-group`：详情 `bi-eye`、取消 `bi-stop-circle`（仅图标）。
- **状态**：已完成

### Q111 · 任务中心 · 表格再收紧

- **问题**：列表仍出现底部横向滚动条；摘要列过宽（38%），长文案应进详情查看。
- **处理**：
  1. 列宽改为全百分比合计 100%，摘要压至 22%；去掉操作列 `72px`。
  2. `table-responsive` 改为 `task-center-table-wrap`（`overflow-x: hidden`）；单元格统一裁剪/省略。
  3. 摘要去掉长 `title` 悬停，完整内容点详情查看。
- **状态**：已完成

### Q112 · 任务中心 · 摘要主机截断

- **问题**：配置批量同步等任务摘要把全部主机名串在一行。
- **处理**：`format_task_center_summary` 对 `target_hostnames` 默认最多展示 3 台，超出显示「a,b,c 等N台」；完整节点列表在详情页目标节点区查看。首页最近任务共用同一摘要函数。
- **状态**：已完成

### Q113 · Nginx 升级 · 向导再进入从第 1 步

- **问题**：停在第 3 步编译参数后离开再进升级中心，会自动回到第 3 步并带上草稿。
- **处理**：`restoreUpgradeWizardState` 强制第 1 步；不恢复节点/源码包/编译参数等草稿；`sessionStorage` 仅保留进行中批次进度以便续看；去掉为恢复第 3/4 步预拉 `nginx -V`。
- **状态**：已完成

### Q114 · 节点 · 离线禁止同步与发布

- **问题**：`status` 为 `offline`/`unknown` 时仍可配置同步、发布与回滚，与升级中心「仅 online」门禁不一致。
- **处理**：
  1. 同步：`ConfigSyncSingle/BatchAPIView`、`_sync_one`、`ConfigGlobPreviewView` 校验 `status==online`；同步页禁用非在线勾选与按钮。
  2. 发布：`ReleaseCreateAPIView` 跳过非在线 binding；`_execute_node_release_batch` / 重试硬失败；发布中心 `canSelect` 仅 online。
  3. 回滚：`ReleaseRollbackView` / `_start_rollback_for_release_tasks` 拒绝非在线；历史/详情/回滚页隐藏或禁用入口。
- **状态**：已完成

### Q115 · 任务中心 · 失败原因多行格式化

- **问题**：升级等失败原因经 `short_error_tail` 用 ` | ` 压成单行后，详情执行结果难读。
- **处理**：`split_failed_item` / `split_error_reason_lines` 解析；详情树标签与失败原因分行展示（不改存储协议）。
- **状态**：已完成

### Q116 · 权限 · 无权访问自定义弹窗

- **问题**：无权访问返回纯文本 403；首页快捷入口/统计卡无权限也可点进业务页。
- **处理**：
  1. `forbidden_response` 页面请求写入 session 并回首页；`showAlert` 展示「无访问权限」说明。
  2. AJAX 仍 JSON 403；文案统一为联系管理员分配权限。
  3. 首页快捷按钮与统计卡跳转按权限收口（无权限保留只读数字）。
- **状态**：已完成

### Q117 · 用户/角色 · 权限矩阵勾选与全选按钮

- **问题**：角色创建/编辑、用户直授权限矩阵无法勾选；全选按钮默认误显「取消」。
- **处理**：
  1. `allChecked` 要求 `length > 0`，空集合不再 vacuous `every()` 误判为已全选；无 checkbox 时按钮 disabled。
  2. `initPermMatrix` 抽到 `base.html` 全局；支持点击单元格切换勾选。
  3. `UsersConfig.ready()` 按 `all_permission_items()` `get_or_create` 补齐 `PermissionItem`，避免格子只显示「-」。
- **状态**：已完成

### Q118 · Nginx 升级 · 源码包列表展示描述

- **问题**：上传源码包时可填描述，列表未展示。
- **处理**：`package_list.html` 在包名称与版本之间增加「描述」列；截断 + `title` 悬停全文；空描述显示 `-`。
- **状态**：已完成

### Q119 · 审计 · 绑定/版本 detail 可读化

- **问题**：`更新配置绑定` / `创建绑定版本` 的 detail 仅含主键 id，无法看出配置名、节点、版本号。
- **处理**：`signals._get_instance_label` 对 `ConfigNodeBinding` / `BindingVersion` 拼 `配置名 @ 主机名` 与 `· Vn`（remark 非空则括号追加并截断）；不写正文 diff、不合并双日志、不回填历史。
- **状态**：已完成

### Q120 · Nginx 升级 · 第三方模块双通道

- **问题**：目标机已有 `nginx-modules/{name}` 时 `git clone` 失败；无外网节点无法在线下载第三方模块。
- **处理**：
  1. 在线：目录不存在则 clone；已存在则检查分支 → 不一致则 checkout → `git pull`。
  2. 新增 `NginxThirdPartyModulePackage` 与 `/upgrade/modules/` 上传管理（对齐源码包）。
  3. 升级向导第三方模块支持「在线 Git / 离线包」；离线由平台 SFTP 下发并解压。
  4. 准备完成后补齐 `module_path` 与 `--add-module`；Git 失败提示可改用离线包。
- **状态**：已完成

### Q121 · Nginx 升级 · 调整编译模块已编译项可搜索

- **问题**：`BUILTIN_ADD_MODULES` 含 `--with-stream` 等，但弹窗左侧过滤掉节点已启用项，搜索「可选」侧找不到。
- **处理**：左侧展示全部官方参数；已编译项 disabled +「已编译」徽标仍可被搜索命中；交叉空结果提示；文案澄清左右栏语义。不改增减业务规则。
- **状态**：已完成

### Q122 · Nginx 升级 · 已编译判定精确匹配

- **问题**：右侧基线含 `--with-stream=dynamic` 时，左侧 `--with-stream` 也被标灰「已编译」（`isBuiltinModulePresent` 将静态与 `=dynamic` 视为同一项）。
- **处理**：`isBuiltinModulePresent` 改为精确字符串匹配；静态与 dynamic 可分别勾选/移除（切换时需右侧去掉旧项并左侧添加新项）。
- **状态**：已完成

### Q123 · 系统设置 · Nginx 升级任务保留天数

- **问题**：历史保留仅覆盖任务中心/发布/审计/登录，升级历史 `NginxUpgradeTask` 无清理。
- **处理**：新增 `system.retention_upgrade_task_days`（默认 90，0=不清理）；`purge_expired_data` 清理过期升级任务并跳过进行中阶段状态。
- **状态**：已完成

### Q124 · 系统设置 · 整数范围校验与包上限

- **问题**：整数设置无 min/max；包大小 label 仅写源码包；默认 500MB 偏大。
- **处理**：
  1. PRESET 整数项补 `min_value`/`max_value`；保存 API 与设置页越界 Toast。
  2. `upgrade.package_max_size_mb` label 改为「源码包/第三方模块包…」，默认 20；seed 仅当仍为旧默认 500 时改写为 20。
- **状态**：已完成

### Q125 · 节点列表 · 探测时间

- **问题**：列表需展示上次探测成功时间（非通用 `updated_at`）。
- **处理**：`Node.last_probe_at`；`mark_node_probe_success` 统一写入（单/批测、解锁、系统信息/Nginx 版本、凭证启用测）；列表「探测时间」列与详情展示；失败不清除。
- **状态**：已完成

### Q126 · 节点 · 周期性自动 SSH 测试

- **问题**：是否定期自动 SSH 测活。
- **处理**：结论：暂不实现。无独立调度框架；结果直接驱动 Q114 门禁，误报代价高。连通性依赖手动/批量测试与解锁/启凭证路径。
- **状态**：已关闭（结论）

### Q127 · 配置管理 · 周期性自动配置同步

- **问题**：是否周期性自动发现/同步配置。
- **处理**：结论：暂不实现。延续 Q86（不做漂移巡检）；配置变更保持人工触发同步向导。
- **状态**：已关闭（结论）

### Q128 · Nginx 升级 · 首页去掉「上传源码包」快捷入口

- **问题**：升级首页顶栏同时有「上传源码包」「源码包」「模块包」，上传与列表入口重复。
- **处理**：去掉首页「上传源码包」按钮；上传仍从源码包/模块包列表进入；路由与上传页保留。
- **状态**：已完成

### Q129 · Nginx 升级 · 首页去掉重复标题

- **问题**：页头 `page_title` 与下方卡片 `h5` 均为「Nginx 编译升级」。
- **处理**：去掉卡片头左侧重复标题；保留右对齐操作按钮（开始升级 / 源码包 / 模块包 / 历史）。
- **状态**：已完成

### Q130 · Nginx 升级 · 首页操作条布局

- **问题**：去标题后仅剩右对齐按钮的空壳卡片，左侧大片空白不自然。
- **处理**：对齐仪表盘紧凑入口：删除空壳卡片，改为内容区顶部 `btn-sm` 按钮行，直接接统计卡。
- **状态**：已完成

### Q131 · 运维工具 · Nginx 启停

- **问题**：需要独立运维入口对节点执行 Nginx start/stop/reload/restart。
- **处理**：
  1. 侧栏「运维工具」下与「Nginx 升级」同级新增「Nginx 启停」；应用 `apps/nginx_service`。
  2. 独立页多选在线节点 + 启动/停止/重载/重启；异步 `TaskCenterTask(operation_type=nginx_service_control)`；复用 `utils/nginx_ops`（含 `reload_nginx`）。
  3. 权限：看页 `nodes.read`、执行 `nodes.update`；批量上限复用 `node.batch_max_count`。
  4. 门禁：online、未锁定、启用凭证；stop 不改节点 SSH status；重载 ≠ 重启（文案区分）。
  5. 执行走全局 `#asyncProgressOverlay` 轮询（串行逐节点刷 `current_steps` + 活结果树）；「完整日志」跳任务详情。
- **状态**：已完成

### Q132 · 运维工具 · Nginx 全新安装

- **问题**：目标机无 Nginx 时需独立安装入口，且不能影响已落地的升级流水线。
- **处理**：
  1. 侧栏新增「Nginx 安装」；独立应用 `apps/nginx_install` + `run_install_task`（**不**调用/修改 `run_upgrade_task`）。
  2. 三步向导：选节点+源码包 → 编译参数（无 `-V`）→ 确认；复用升级源码包/模块包。
  3. 流水线：configure/make/make install → `nginx -t` → **start**；回写 `nginx_version`/`nginx_path` + `mark_node_probe_success`；写 `main_conf_path` 后自动配置同步。
  4. 同步失败不否定安装、不回滚；历史/弹窗引导手动同步。
  5. 升级向导隐藏「全新安装」选项，引导至本模块。
- **状态**：已完成

### Q133 · 运维工具 · 安装进包管理侧栏高亮

- **问题**：安装页点「源码包/模块包」进入共享的 upgrade 包管理页后，侧栏高亮跳到「Nginx 升级」。
- **处理**：安装入口带 `?nav=nginx_install`；`base.html` 白名单覆盖 `data-nav`；包列表/上传/删除 redirect 透传 `nav`（`utils/nav_context.py`）。
- **状态**：已完成

### Q134 · Nginx 安装 · 历史对齐升级

- **问题**：安装历史仅有状态筛选，缺少搜索与每页条数。
- **处理**：对齐升级历史：tag 搜索（主机/IP/版本/路径/批次）、虚拟「进行中」+ 全状态、`PerPagePaginationMixin`；详情仍跳任务中心。
- **状态**：已完成

### Q135 · Nginx 启停 · 独立历史页

- **问题**：启停仅有「任务中心」外链，缺少与安装一致的历史入口。
- **处理**：新增 `/nginx-service/history/`，列表基于 `TaskCenterTask(operation_type=nginx_service_control)`；首页「历史」按钮；搜索/状态/分页对齐安装历史。
- **状态**：已完成

### Q136 · Nginx 安装 · 向导编译参数/确认对齐

- **问题**：安装第二步仅为简单勾选网格，第三步摘要过简；缺 `--user`/`--group` 与可配置缺省 `--prefix`。
- **处理**：
  1. 第二步左右栏：左支持模块（`BUILTIN_ADD_MODULES` + 搜索），右基础参数（user/group/prefix/工作目录/make -j）。
  2. 系统设置「安装管理」：`install.default_user`/`default_group`（默认 root）、`install.default_prefix`（默认 `/opt/app`）；右栏缺省读设置、可改。
  3. 第三步对齐升级确认：KV、configure 预览、节点清单、确认勾选。
- **状态**：已完成

### Q137 · Nginx 启停 · 去 hint + 最近任务

- **问题**：目标节点旁「仅可选在线且已配置凭证…」冗余；首页无最近任务。
- **处理**：删除该 `action-hint`；首页新增「最近启停任务」表（链任务详情）；条数见 Q138。
- **状态**：已完成

### Q138 · 运维工具 · 最近任务条数统一

- **问题**：升级首页 10、安装首页 20、启停无列表，口径不一致。
- **处理**：三首页统一读 `dashboard.recent_tasks_count`（`get_recent_tasks_limit`）；更新该设置项 description。
- **状态**：已完成

### Q139 · Nginx 安装 · 向导布局与进度日志

- **问题**：编译参数左右双栏不适合运维扫读；无第三方模块 UI；开始安装后进度弱于升级中心，无专用日志页动态刷新。
- **处理**：
  1. Step2 改为纵向：顶部紧凑基础参数 → 全宽支持模块 → 第三方（在线 Git / 离线包）。
  2. 创建前 `enrich_third_party_module_paths`；确认页展示第三方与 `--add-module` 预览。
  3. 中心左右栏：右 sticky「安装执行进度」节点块；「查看完整日志」→ `/nginx-install/task/<id>/log/`。
  4. 日志页「完整执行日志」轮询 `api_task_log`；首页/历史详情入口改链该页。
- **状态**：已完成

### Q140 · Nginx 安装 · 编译参数模块调整对齐升级

- **问题**：Step2「额外 configure」标签冗余；支持模块页内网格搜索会顶滑；交互与升级「调整模块」左右弹窗不一致。
- **处理**：
  1. 删除「额外 configure 参数（可选，每行一个）」label，保留 `#extraOpts` textarea。
  2. 支持模块改为摘要条 +「调整模块」`modal-xl` 左右栏（绝对选集：左可选/右已选；已选项左侧 disabled +「已选」徽标仍可搜）。
  3. 弹窗搜索用 `display` 显隐过滤，避免 `innerHTML` 重建导致页面顶滑；`added_modules` 仍为已选官方模块 + extraOpts 行。
  4. 勾选仅记增减草稿（基线=`selectedModules` 打开快照），确认后提交；不即时左右挪栏（对齐升级）。
- **状态**：已完成

### Q141 · Nginx 安装 · 开跑去掉全局进度遮罩

- **问题**：开始安装仍弹全局 `#asyncProgressOverlay`（「Nginx 安装 / 完整日志」），与中心右栏「安装执行进度」重复。
- **处理**：`startBatchPoll` 不再调用 `showAsyncProgressOverlay` / `startAsyncProgressPolling`；仅右栏批次轮询刷新节点进度与「查看完整日志」。
- **状态**：已完成

### Q142 · Nginx 安装 · 进度展开态与失败原文

- **问题**：多节点安装时轮询整表重建且仅首台默认展开，点开失败节点详情会被刷掉；失败文案用 `.small` 字号偏大，且进度 API 截断 300 字。
- **处理**：
  1. `renderBatchProgress` 重建前保留已展开 `data-task-id`；首次仍默认展开第一台。
  2. 失败信息用 `.install-step.failed` 直出原文（`pre-wrap`）；批次进度 API `error_message` 不再 `[:300]`。
- **状态**：已完成

### Q143 · Nginx 安装 · 失败截断与详情编译参数

- **问题**：进度区失败全文过长；任务详情标题为「configure 参数」且缺 `./configure` 前缀。
- **处理**：
  1. 进度失败对齐升级：`truncateErrorLines` 最多 5 行 +「失败:」前缀；完整错误见日志页。
  2. 任务详情标题改为「编译参数」；展示前加 `./configure \`（不改库内字段）。
- **状态**：已完成

### Q144 · Nginx 安装 · 批次 IN- 日自增与历史跳转

- **问题**：安装批次为随机 `NI-YYMMDD-XXXX`；任务中心来源批次不可跳转；首页最近任务无批次列。
- **处理**：
  1. 删除随机 `NI-` 生成；`generate_install_batch_number` 产出 `IN-YYMMDD-NNNN`（当日自增）。
  2. 任务中心详情「来源批次」链 `/nginx-install/history/?search=`。
  3. 安装首页「最近安装任务」增加批次列（可点跳历史）。
- **状态**：已完成

### Q145 · Nginx 启停 · 批次 OP- 与三首页最近任务批次

- **问题**：启停无来源批次；历史不可按批搜；升级/安装/启停首页最近任务均无批次列。
- **处理**：
  1. 创建启停任务写入 `source_batch=OP-YYMMDD-NNNN`（当日自增）。
  2. 启停历史 search 含 `source_batch`，列表增批次列；任务详情链启停历史。
  3. 升级/安装/启停三首页「最近任务」统一展示批次并可跳对应历史。
- **状态**：已完成

### Q146 · Nginx 安装 · 失败回填任务中心结果树

- **问题**：安装失败时 `fail()` 仅写 `detail`，未写 `result`，任务详情「执行结果」显示「暂无结果数据」；成功路径已有结果树。
- **处理**：`fail()` 对齐升级：写入 `build_tree_result`（节点 + `item_failed`/`short_error_tail`），并清理进度态。
- **状态**：已完成

### Q147 · 发布未运行则 start · 安装 listen 与非 root 告警

- **问题**：非 root 安装时默认 listen 80 导致 `nginx -t` bind 失败、进程未启动；随后改端口再发布时 `nginx -s reload` 因空 PID 失败并整批回滚。
- **处理**：
  1. `reload_nginx(start_if_stopped=True)`：未运行则改为 `start`；发布统一 reload 与升级 reload 启用；启停页显式「重载」仍纯 reload。
  2. 系统设置 `install.default_listen_port`（默认 80）；向导可改；装后写主配置 `listen`（非 configure）。
  3. 编译参数「下一步」：listen=80 且存在非 root SSH 凭证节点时整批 `showConfirm` 告警（可继续进确认页）；节点搜索 API 返回 `credential_username`。
  4. `-t` 遇 Permission denied/bind 时补充非 root 特权端口引导文案。
- **状态**：已完成

### Q148 · 任务摘要 · 批次号与仪表盘截断

- **问题**：发布类摘要主行为批次号，升级/安装/启停仍优先主机名；仪表盘摘要副行无截断，长错误显示杂乱。
- **处理**：
  1. `format_task_center_summary`：`nginx_upgrade` / `nginx_install` / `nginx_service_control` 主行优先 `source_batch`，无批次再回退主机名。
  2. 任务中心与仪表盘对上述类型主行用 `<code>`；仪表盘摘要主/副行加 `text-truncate`（对齐任务中心，全文进详情）。
- **状态**：已完成

### Q149 · UI · 样式/布局/弹窗一致性收敛

- **问题**：双主色（Bootstrap 蓝 vs 品牌紫）；启停页 `extra_css` 未挂入；`showConfirm` 富内容默认过窄；发布/回滚自建进度层与全局 overlay 重复；内容预览尺寸分裂；残留 `alert()`；弹窗页脚按钮大小不一。
- **处理**：
  1. 启停模板改 `extra_head`；chip 改矩形对齐升级/安装。
  2. `showConfirm`：`asHtml` 默认 `md`、含 table 默认 `lg`；任务取消/用户操作改 `showAlert`；`.modal-footer` 统一小号按钮。
  3. 发布/回滚进度改用全局 `#asyncProgressOverlay`（支持批次号、跳过失败、`onClose`）；删除本地 `.publish-overlay`。
  4. 发布中心内容预览改为 `modal-xl`；`--bs-primary` 对齐 `--primary`；pending 筛选 chip 用品牌色。
  5. 规范写入 [`docs/13-ui-conventions.md`](docs/13-ui-conventions.md) §5.1。
- **状态**：已完成

### Q150 · 节点 · SSH/Nginx 双维度状态与门禁

- **问题**：`Node.status` 仅表示 SSH 可达；Nginx 卸载后节点仍「在线」，配置仍「已同步」，同步/发布/升级/启停可勾选导致歧义与运行时失败。
- **处理**：
  1. 新增 `nginx_available`（null/True/False）与 `last_nginx_probe_at`；SSH 状态与 Nginx 徽标分开展示。
  2. `apply_nginx_probe_result` 统一写入；SSH 测/版本采集共用；失败清空版本并 `mark_node_bindings_orphaned`（用 `orphaned` 非 `marked_deleted`）。
  3. 门禁：offline/unknown 全禁；online+Nginx 可用才同步/发布/回滚/升级/启停；安装仅需 online；确认页对已有 Nginx 弱提示。
  4. 配置列表无 Nginx 节点仅可查看/解除绑定；装回后不自动变 `synced`。
- **状态**：已完成

### Q151 · UI · Nginx 状态徽标展示对齐

- **问题**：发布中心「未检测到 Nginx」与绑定数重叠；升级中心「在线」与 Nginx 徽标换行且文案长于安装/启停；配置列表徽标旁重复长句显怪。
- **处理**：
  1. `nginx_status_label` 短文案改为「未检测到」（错误提示句仍用完整「未检测到 Nginx…」）。
  2. 发布中心状态列加宽；升级中心状态 `text-nowrap` + 独立短徽标。
  3. 配置列表去掉旁路长句；`_nginx_badge` 支持可选 `badge_title`，悬停说明「仅可查看/解除绑定」。
- **状态**：已完成

### Q152 · 运维工具 · Nginx 卸载

- **问题**：运维工具缺独立卸载入口；仅靠探测失败 orphan，无法主动删除 `--prefix` 与远程发布备份。
- **处理**：
  1. 侧栏新增「Nginx 卸载」；应用 `apps/nginx_uninstall`；首页对齐升级运维台；中心三步向导（选择目标 → 探测路径 → 确认执行）+ 右栏「卸载执行进度」（对齐升级，不用全局 overlay）。
  2. 门禁对齐升级：仅 online + 凭证 + `nginx_available`；「未检测到」不可选。
  3. 探测 `nginx -V` 路径参数与系统设置项（备份/工作目录/modules）统一列表按节点勾选；节点块可折叠。
  4. 流水线 stop → 删 prefix → 按勾选清备份/额外路径；回写清空路径/版本，`apply_nginx_probe_result(False)` + orphan；清空 `main_conf_path`。
  5. TaskCenter `nginx_uninstall`，批次 `UN-YYMMDD-NNNN`；批次进度轮询 + 取消级联；历史/摘要对齐现有运维模块。
- **状态**：已完成

### Q153 · 安装/卸载 · systemd 管理能力分流

- **问题**：源码安装后常无 unit，启停靠二进制；卸载明确「不做删除 systemd unit」，导致 root 安装无法开机自启、卸载后 unit 残留。
- **处理**：
  1. `utils/nginx_ops`：`can_manage_systemd_unit`（可写 `/etc/systemd/system` 或 `sudo -n`）、写/删 `nginx.service`、`start_nginx(prefer_mode, use_sudo)`。
  2. 安装：`nginx -t` 后能管则写 unit（`User=`/`Group=`←`--user`/`--group`）+ enable + start；不能则二进制；写/enable/start 失败不静默降级。确认页与开始安装弹窗说明策略；含非 root 时提示权限差异。
  3. 卸载：preview 返回 `manage_mode`/`credential_username`；systemctl 托管则 `removing_unit`（disable + 删可管 unit）；二进制跳过；非 root+systemctl 与停服合并一次风险确认。不删发行版 `/lib`/`/usr/lib` unit。
- **状态**：已完成

### Q154 · 卸载 · 包安装走 yum/apt remove

- **问题**：yum/apt 安装的 Nginx 若按源码路径 `rm -rf` 易误删发行版目录，且与包管理状态不一致；文档曾写「不做 apt/yum」。
- **处理**：
  1. `detect_nginx_package_origin`：`rpm -qf` / `dpkg -S` 归属二进制 → 包名；否则源码。
  2. 包路径：`dnf`/`yum`/`apt-get remove -y`（需系统权限/`sudo -n`），不 rm `--prefix`/`-V` 路径；可选备份/工作目录仍可清；残留平台 `/etc/systemd/system/nginx.service` 再清。
  3. 源码路径维持删树 + Q153 systemd。preview/确认展示安装来源徽标；状态 `removing_package`。
- **状态**：已完成

### Q155 · Nginx 卸载 · 探测并行与路径包含勾选

- **问题**：多节点第 2 步路径探测串行且每台双 SSH，等待随节点数线性增长；探测页对真包含子路径无父子勾选联动。
- **处理**：
  1. `preview_nodes` 按 `node.batch_max_count` 线程池并行；单节点一次 SSH 完成 `-V`/包归属/托管/运行态；结果按节点 id 排序。
  2. 同路径仍精确去重；真包含路径保留展示：父勾选 → 子自动勾选并「随父路径」锁定；取消父勾选 → 子解锁并取消勾选；编辑 `--prefix` 后重算。
  3. 执行侧 `_under_prefix` / `coalesce_delete_targets` 不变。
- **状态**：已完成

### Q156 · 批量勾选上限统一与冗余并发设置下线

- **问题**：升级/发布中心总勾选不受 `node.batch_max_count` 约束；配置同步总勾选会勾上 disabled（未检测到 Nginx）；卸载总勾选超限弹窗；「最多勾选」黄/黑文案不一；`release.max_parallel_tasks` / `config.sync_max_concurrency` / `credential.test_max_concurrency` 与批量上限语义重叠。
- **处理**：
  1. 总勾选静默截断至上限；单勾超限统一黄字「最多只能勾选 N 个节点」。
  2. 升级/发布/配置同步 UI + API 勾选上限接 `node.batch_max_count`；同步全选跳过 disabled。
  3. 发布/同步/升级/凭证启用跨节点并行 worker 一律读 `node.batch_max_count`（凭证仍测全部关联节点）。
  4. PRESET 下线三项冗余键；更新 `node.batch_max_count` 说明与 docs。
- **状态**：已完成

### Q157 · 用户管理 · 密码文案 / 侧栏 / 运维独立权限

- **问题**：
  1. 密码相似+过短时 Django 中文多条拼接，文案需统一。
  2. 编辑普通用户时侧栏「用户管理/系统设置」消失（非 sticky）：`UserUpdateView`/`UserDeleteView` 默认 `context_object_name=user` 覆盖登录用户，`base.html` 按被编辑对象权限画菜单。
  3. Nginx 安装/启停/卸载无独立权限矩阵行（安装挂 `upgrade.*`，启停/卸载挂 `nodes.*`）。
  4. 权限矩阵资源列文案过简（「节点」「凭证」等）。
- **处理**：
  1. `CombinedSimilarityAndLengthValidator` 合并相似度与最短长度为「密码不能与用户名过于相似，且长度不能少于 8 个字符。」；保留常见/纯数字校验。
  2. 侧栏 sticky + `scrollIntoView`；`UserUpdate`/`Delete` 设 `context_object_name=edit_user`；`base.html` 侧栏一律 `request.user|has_perm_code`。
  3. `perm_defs` 增 `nginx_install` / `nginx_service` / `nginx_uninstall`；视图/侧栏/任务中心/仪表盘改接新码（`read`=看、`create`=跑）；迁移 `0003` 从 `upgrade.*`/`nodes.*` 拷贝授权。
  4. `RESOURCE_CHOICES` 资源列对齐侧栏：节点管理/凭证管理/配置管理/发布管理/用户管理/角色管理/用户组管理/审计日志（Nginx 项与系统设置不变）。
- **状态**：已完成

### Q158 · 节点/凭证 · 列表 xlsx 导出

- **问题**：节点/凭证列表缺少导出；运维备份需拿回导入兼容的节点表，以及含明文密码/私钥的凭证表。
- **处理**：
  1. 节点：`GET /nodes/export/`（`nodes.read`），按当前筛选导出；表头对齐批量导入；凭证列仅名称；审计「导出节点」。
  2. 凭证：`GET /credentials/export/`（`credentials.read`），确认弹窗后导出明文密码/私钥（按认证方式只填对应列）；审计「导出凭证」仅条数/名称、不落密。
- **状态**：已完成

### Q159 · 节点/凭证 · 导出勾选与凭证导入

- **问题**：导出需支持勾选子集；未勾选全量应二次确认；凭证缺少与导出对称的批量导入。
- **处理**：
  1. 节点/凭证导出：有勾选仅导 `ids`；未勾选弹窗确认后按筛选全量导出。
  2. 凭证列表增加勾选列与「批量导入」：表头对齐导出；整文件校验；当前用户同名更新否则新建；审计「导入凭证」。
- **状态**：已完成
